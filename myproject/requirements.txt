# Django-related libraries
Django>=5.1.3
djangorestframework>=3.14.0  # For building REST APIs
django-cors-headers>=3.13.0  # For handling CORS in Django
django-environ>=0.10.0  # For environment variable management

# Database support
psycopg2>=2.9.6  # PostgreSQL adapter

# Production WSGI server
gunicorn>=20.1.0

# Optional (if you use whitenoise for serving static files in production)
whitenoise>=6.5.0